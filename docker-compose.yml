services:
  web:
    build:
      context: .
      target: production
    ports:
      - "8080:80"
    restart: always

  dev:
    image: node:24-alpine
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "5173:5173"
    command: npm run dev -- --host
